import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#FFFFFF',
        position: 'absolute',
        zIndex: 1,
    },
    radioButtons: {
        flexWrap: 'wrap',
    },
});

export { styles };
