import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#FFFFFF',
        flexGrow: 1,
    },
    description: {
        width: '80%',
    },
    badgerWrapper: {
        gap: 10,
    },
});

export { styles };
