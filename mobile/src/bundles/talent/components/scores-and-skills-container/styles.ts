import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tagsWrapper: {
        flexWrap: 'wrap',
        gap: 6,
    },

    badgesWrapper: {
        flexWrap: 'wrap',
        gap: 8,
    },
});

export { styles };
