import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    backdrop: {
        backgroundColor: 'rgba(0,0,0,0.5)',
    },
    container: {
        width: '80%',
        gap: 20,
        backgroundColor: '#fff',
    },
    content: {
        gap: 10,
    },
});

export { styles };
