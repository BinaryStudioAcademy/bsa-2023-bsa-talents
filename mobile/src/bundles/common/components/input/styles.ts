import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#7e7e7e',
        fontSize: 15,
    },
    label: {
        color: '#000',
        fontSize: 15,
    },
    disabled: {
        opacity: 0.5,
    },
    disabledLabel: {
        opacity: 0.4,
    },
});

export { styles };
