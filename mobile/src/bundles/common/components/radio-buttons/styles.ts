import { StyleSheet } from 'react-native';

import { Color } from '../../enums/enums';

const styles = StyleSheet.create({
    container: {
        flexWrap: 'wrap',
    },
    label: {
        color: Color.TEXT,
    },
});

export { styles };
